前端重构注意：

1.怎么知道具体的操作逻辑；
2.怎么知道隐性的数据传递；
3.现有的api  cookie  自动完成  用户状态保存

*以www项目为例，
   *所有www相关的静态资源放在ui597img/www下，里面有css、images、js三个目录
   *js里跟进模块再进行目录划分，如zhaopin
   *目前增加的文件或文件夹有
      I:\www\js\zhaopin
      I:\www\js\jqExtend.js
   *插件抽取，重点在于使用方式的调研
      初始化属性抽取到options  这些属性是作用
      事件初始化直接采用on方式，具体有多少事件可注册可在源码插件里找trigger
   *搜索页面会showFilter有bug  因为html字符串引起
    链接的登录提示页需要再另外整理

*目前需要整理的页面：
搜索页 http://xm.597.cs/zhaopin/c2/?q=
首页
公司页
职位页

最新招聘 http://xm.597.cs/companyjob.html



*首页延迟加载方案
 *各个模块有可能有数据 有可能没数据  判断容器是否在clientHeight+200px范围内（一次滚轮大概在200px以内），如果是这加载数据。
 *各个模块单次加载的量多少自定义，目前可以用全加载
 *刚开始时延迟加载的模块内数据为空，height=0   如何保证在加载完前一个模块后再加载下一个模块？
  可先读取预加载模块数据总量后先放好占位符li>img 后面填充真实src。？但是如金牌企业招聘2模块的图片宽度不一致。 量少不予考虑
 *统一在预加载模块dom上打上class lazyload data-api=
  160*60
  118*45
  139*35

*公司首页
 周一或周二将cny_flag_end内容去掉后做页面效果检查
 再做下部分js合并，原本有31个js现在24个
 
！！中间插入做融云、图片裁切器、修改前端bug 耗费了一些是时间